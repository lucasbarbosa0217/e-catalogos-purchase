import styled from "styled-components";
import FooterWrapper from "./footerContainer";

const FooterContainerQuantity = styled(FooterWrapper)`

    background-color: var(--background-color);
  
`;

export default FooterContainerQuantity;