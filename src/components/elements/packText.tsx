import styled from "styled-components";

const PackText = styled.span`
   position: absolute;
   top: -1.1rem;
   font-size: 0.9rem;
   color: var(--background-color);
    
`;

export default PackText;